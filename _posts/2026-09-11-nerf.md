---
layout: post
title:  "[논문정리] NeRF: Representing Scenes as Neural Radiance Fields for View Synthesis"
date:   2026-09-11 10:17
categories: AI CV
tag: CV 
math: true
---




> 서지정보
> Mildenhall, Ben, Pratul P. Srinivasan, Matthew Tancik, Jonathan T. Barron, Ravi Ramamoorthi와/과Ren Ng. “NeRF: Representing Scenes as Neural Radiance Fields for View Synthesis”. arXiv:2003.08934. Preprint, arXiv, 2020년 8월 3일. [https://doi.org/10.48550/arXiv.2003.08934](https://doi.org/10.48550/arXiv.2003.08934).
{: .prompt-tip }

  

> 초록(Abstract)
> We present a method that achieves state-of-the-art results for synthesizing novel views of complex scenes by optimizing an underlying continuous volumetric scene function using a sparse set of input views. Our algorithm represents a scene using a fully-connected (non-convolutional) deep network, whose input is a single continuous 5D coordinate (spatial location $(x,y,z)$ and viewing direction $(θ, φ)$) and whose output is the volume density and view-dependent emitted radiance at that spatial location. We synthesize views by querying 5D coordinates along camera rays and use classic volume rendering techniques to project the output colors and densities into an image. Because volume rendering is naturally differentiable, the only input required to optimize our representation is a set of images with known camera poses. We describe how to effectively optimize neural radiance fields to render photorealistic novel views of scenes with complicated geometry and appearance, and demonstrate results that outperform prior work on neural rendering and view synthesis. View synthesis results are best viewed as videos, so we urge readers to view our supplementary video for convincing comparisons.
{: .prompt-info }


### 3 Neural Radiance Field Scene Representation

![alt](/assets/img/nerf.fig1.png)



**(a) 5D input : Position + Direction**  
카메라에서 특정 픽셀을 향해 ray를 하나 쏜다.  
ray 위 여러 점을 샘플링한다.  
그림의 검은 점들이 샘플들이다. 각 점마다 NeRF에 넣는 입력은 (x,y,z,θ,ϕ)으로 ray 위의 3D 위치와 그 점을 바라보는 viewing direction이다.   
한 ray 전체를 한번에 MLP에 넣는게 아니라 ray위의 여러 3D point를 각각 queary 한다. 

**(b) Output: color + Density**  
각 sampled point를 MLP $F_Θ$ 에 넣으면 RGB와 σ 가 나온다

RGB는 그 위치가 해당 viewing direction에서 어떤 색으로 보이는지 나타낸다.   

σ는 그 위치에 물체가 얼마나 강하게 존재하는지를 나타낸다. 이 값이 크면 이 지점에서 ray가 부딪힐 가능성이 높다고 본다. 

densitys는 보는 방향에 무관하지만 색은 viewing direction에 따라 달라질 수 있다.

**(c) Volume Rendering**   
x축은 ray distance이고  
y축은 density이다. 

그냥 density가 가장 큰 점의 RGB를 가져오는 것이 아니다. ray위의 모든 포인트를 보고 실제로 보일 가능성이 높은 point들의 색을 섞어서 한 pixel을 만든다. 

**(d) Rendering Loss**   
Volume Rendering으로 예측 pixel을 만들어 실제 사진 pixel과 비교한다. 


>NeRF는 장면을 MLP 기반의 implicit radiance field로 표현해 ray sampling과 volume rendering으로 픽셀을 만들고   
>3DGS는 장면을 explicit 3D Gaussians로 표현해 projection과 resterization으로 이미지를 만든다
{: .prompt-warning }


> Rasterization  
3D 장면의 물체를 2D 화면의 픽셀로 바꾸는 과정. 
3D공간에 있는 Gaussian들을 카메라 평면으로 투영한 뒤, 어떤 가우시안이 어떤 픽셀에 얼마나 영향을 주는지 계산해서 이미지를 만드는 과정이다. 
{: .prompt-info }
